<html>
	<head>
		{{ include('common/headers.twig') }}
	</head>
	<body>
		{{ include('common/main_nav.twig') }}

		<div class="container dashboard_con">

			<section class="quick_create_user_section bg-light">

				<h3 class="section_header">Plan Info</h3>

                <div class="row create_plan_con">
                    <div class="col-12 col-md-12">

                        <div class="form-row">
                            <div class="form-group col-md-6">
                                <label for="inputEmail4">Name</label>
                                <input type="text" class="form-control" id="first_name" value="{{customer.first_name}}" placeholder="First Name">
                            </div>
                            <div class="form-group col-md-6">
                                <label for="inputState">Type</label>
                                <select id="sex" class="form-control">
                                    <option value="{{customer.sex}}">Choose...</option>
                                    <option value="male">Male</option>
                                    <option value="female">Female</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-row">
                            <div class="form-group col-md-12">
                                <label for="inputEmail4">Description</label>
                                <input type="text" class="form-control" id="email" value="{{customer.email}}" placeholder="Email">
                            </div>
                        </div>

                        <button onclick="updateUser({{customer.id}})" type="button" class="btn btn-primary">Update</button>
                    </div>
                </div>

			</section>

			<section class="quick_create_user_section bg-light">

				<h3 class="section_header">Plan Workout</h3>

                <div class="row create_plan_con">
                    <div class="col-12 col-md-12">

                        <div class="form-row">
                            <div class="form-group col-md-6">
                                <input type="text" class="form-control" id="first_name" disabled="true" value="Monday" placeholder="First Name">
                            </div>
                            <div class="form-group col-md-6">
                                <select id="sex" class="form-control">
                                    <option value="{{plan.monday}}">{{plan.monday}}</option>
                                    {% for data in workouts %}
                                    	<option value="male">{{data.title}}</option>
                                    {% endfor %}
                                </select>
                            </div>
                        </div>


                        <div class="form-row">
                            <div class="form-group col-md-6">
                                <input type="text" class="form-control" id="first_name" disabled="true" value="Monday" placeholder="First Name">
                            </div>
                            <div class="form-group col-md-6">
                                <select id="sex" class="form-control">
                                    <option value="{{plan.monday}}">{{plan.monday}}</option>
                                    {% for data in workouts %}
                                    	<option value="male">{{data.title}}</option>
                                    {% endfor %}
                                </select>
                            </div>
                        </div>


                        <div class="form-row">
                            <div class="form-group col-md-6">
                                <input type="text" class="form-control" id="first_name" disabled="true" value="Monday" placeholder="First Name">
                            </div>
                            <div class="form-group col-md-6">
                                <select id="sex" class="form-control">
                                    <option value="{{plan.monday}}">{{plan.monday}}</option>
                                    {% for data in workouts %}
                                    	<option value="male">{{data.title}}</option>
                                    {% endfor %}
                                </select>
                            </div>
                        </div>

                        
                        <div class="form-row">
                            <div class="form-group col-md-6">
                                <input type="text" class="form-control" id="first_name" disabled="true" value="Monday" placeholder="First Name">
                            </div>
                            <div class="form-group col-md-6">
                                <select id="sex" class="form-control">
                                    <option value="{{plan.monday}}">{{plan.monday}}</option>
                                    {% for data in workouts %}
                                    	<option value="male">{{data.title}}</option>
                                    {% endfor %}
                                </select>
                            </div>
                        </div>

                        <button onclick="updateUser({{customer.id}})" type="button" class="btn btn-primary">Update</button>
                    </div>
                </div>

			</section>
		</div>
	</body>
</html>